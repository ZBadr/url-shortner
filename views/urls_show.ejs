<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <%- include('partials/_header'); %>

</head>
<%= email %>

<body>
<% if (userId) { %>
  <form method="POST" action="/urls/<%= shortURL %>" style="">
    <label for="longURL">Update URL:</label>
     <input id="longURL" type="text" name="longURL" placeholder="http://" style="width: 300px" required>
     <input type="submit" value="Submit">
  </form>
    <p style= "display: inline;"><%= shortURL %> - <%= longURL %></p>
    <% } else {%>
    <meta http-equiv="refresh" content="0; URL='http://localhost:8080/login'" />
    <% } %>
</body>
</html>